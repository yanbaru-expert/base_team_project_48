<h1>タスク 編集</h1>
<%= form_with model: @task, local: true do |f| %>
  <p>タイトル<%= f.text_field :title %></p>
  <p>詳細<%= f.text_field :detail %></p>
  <%= f.submit "送信" %>
<% end %>
<%= link_to "一覧画面へ", tasks_path %>